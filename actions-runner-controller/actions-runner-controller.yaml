apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: actions-runner-controller
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/actions-runner-controller/actions-runner-controller
    path: charts/actions-runner-controller
    targetRevision: v0.27.4
    helm:
      parameters:
        - name: syncPeriod
          value: 1m
        - name: runner.statusUpdateHook.enabled
          value: "true"
        - name: "actionsMetricsServer.enabled"
          value: "true"
        - name: "actionsMetrics.proxy.enabled"
          value: "true"
        - name: "actionsMetrics.serviceMonitor.enable"
          value: "true"
  destination:
      name: in-cluster
      namespace: actions-runner-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - Replace=true